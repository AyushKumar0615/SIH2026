VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Public VAPID key only — safe to expose client-side (it's not a secret,
# see supabase/functions/send-reminder-push for where the matching private
# key lives, as an Edge Function secret only). Generate a pair with:
#   npx web-push generate-vapid-keys
VITE_VAPID_PUBLIC_KEY=
